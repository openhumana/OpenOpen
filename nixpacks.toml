[phases.setup]
nixPkgs = ["python311Full", "gcc"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 2 main:app"
